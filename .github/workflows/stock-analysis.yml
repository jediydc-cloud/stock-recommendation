name: 주식 분석 자동 실행

on:
  schedule:
    # 평일 오전 8시 (한국시간 기준, UTC 23:00 = 한국시간 08:00)
    - cron: '0 23 * * 1-5'  # 월~금요일만 실행
  
  workflow_dispatch:  # 수동 실행 버튼 (새로고침 기능)

jobs:
  analyze-stocks:
    runs-on: ubuntu-latest
    timeout-minutes: 60  # 전체 작업 타임아웃: 60분
    
    steps:
    - name: 코드 체크아웃
      uses: actions/checkout@v3
      
    - name: Python 설치
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 필요한 패키지 설치
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 주식 분석 실행
      id: analyze
      timeout-minutes: 50  # 분석 단계 타임아웃: 50분
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        python stock_recommendation_with_exchange.py
      continue-on-error: false
        
    - name: 로그 파일 저장 (에러 발생 시)
      if: failure()
      uses: actions/upload-artifact@v4  # 🆕 v3 → v4 업그레이드
      with:
        name: error-logs
        path: |
          *.log
          output/
        retention-days: 7
        
    - name: 분석 결과 업로드
      if: success()
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./output
        publish_branch: gh-pages
        
    - name: 분석 완료 알림
      if: always()
      run: |
        if [ "${{ steps.analyze.outcome }}" == "success" ]; then
          echo "✅ 주식 분석 성공!"
          echo "📊 웹페이지: https://jediydc-cloud.github.io/stock-recommendation/"
        else
          echo "❌ 주식 분석 실패!"
          echo "🔍 로그를 확인하세요."
        fi
